{
  "name": "fx-garanti-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "git archive -o deploy.tar HEAD && caprover deploy -n hetzner -a fx-garanti-ui -t deploy.tar && rm deploy.tar"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.9.0",
    "axios": "^1.8.1",
    "dayjs": "^1.11.19",
    "nuxt": "^3.21.0",
    "pinia": "^2.3.1",
    "vue": "latest"
  },
  "devDependencies": {
    "@nuxtjs/color-mode": "^4.0.0",
    "@nuxtjs/tailwindcss": "^6.13.1",
    "typescript": "^5.7.3"
  }
}
